# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Imported reference material (ECC)

`.claude/agents/`, `.claude/skills/`, and `.claude/rules/ecc/` contain a curated
subset imported from github.com/affaan-m/ecc: TypeScript/React Native coding
standards, plus reviewer/planner/security agents and skills for git, GitHub,
Postgres, TDD, and security review. Not auto-loaded — read the relevant file
when doing non-trivial TS/RN work, a review, or anything security-sensitive.
Start at `.claude/rules/ecc/README.md` for the project-specific caveats
(no test suite yet, no custom backend, agent roster differs from upstream).
